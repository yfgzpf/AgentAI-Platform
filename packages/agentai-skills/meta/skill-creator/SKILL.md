# Skill Creator (技能生成器)

自动生成新的 Python 技能模板，含 SKILL.md、main.py、requirements.txt。

## 参数
- `name` (string, required): 技能名称
- `description` (string): 功能描述
- `category` (string): office / web / image / video / desktop / code / meta

## 执行
本地文件系统操作，无 Docker
